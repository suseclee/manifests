kind: Pod
apiVersion: v1
metadata:
  name: read-pod
spec:
  containers:
  - name: read-pod
    image: gcr.io/google_containers/busybox:1.24
    command:
      - "/bin/sh"
    args:
      - "-c"
      - "test -f /mnt/SUCCESS && exit 0 || exit 1"
    volumeMounts:
      - name: test-nfs-pvc
        mountPath: "/mnt"
  restartPolicy: "Never"
  volumes:
    - name: test-nfs-pvc
      persistentVolumeClaim:
        claimName: nfs-test

